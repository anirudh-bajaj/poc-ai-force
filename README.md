
## Steps to execute in local
    - Unzip the project folder
    - Install python
    - Install vscode or any python supporing IDE  (if not available)
    - Open the project in IDE 


    - in command prompt cd .. C:/DocCompare

# run the command to ativate the environment 'doccompoare' which is specific to this project and already created, and command looks like 
    - C:\DocCompare>conda activate C:\DocCompare\doccompare
    - output will be : (C:\DocCompare\doccompare) C:\DocCompare>

# run the command to import all ncessary libaries for this project requirements.txt is the place to have libraries
    - 'pip install -r requirements.txt' and looks like
    - (C:\DocCompare\doccompare) C:\DocCompare>pip install -r requirements.txt
    - will download all necessary libraries

# Place API keys in '.env' file    
    - This project has two flavours '_gemini' is google gemini, 
    '_openai' for OpenAI need to place valid API Keys

# run the command 'streamlit run app_gemini.py' to execute the application 
    - (C:\DocCompare\doccompare) C:\DocCompare\src>streamlit run app_gemini.py

# data folder source file and target file    






# poc-ai-force
